<!-- CONTINUATION PROMPT START -->
# Continue Previous Session: Capability Metrics And Live Discovery Closure

Read this entire continuation package before doing any work.

## Session Information

- Generated: 2026-05-03 11:12:17 IST
- Repo: `/Users/umasankr/Projects/ping-mem`
- Branch: `main`
- Current worktree: dirty trust-rebuild stack. Do not revert.
- User restart phrase: "continue from where we left off"

## What That Phrase Means

Resume the capability metrics and live discovery closure work.

The immediate objective is not a new feature. It is making ping-mem's capability
claims measurable, visible, and true:

- restore live ingestion service readiness;
- make health/status fail honestly when codebase/graph capabilities are missing;
- inventory every original ping-mem capability from its product purpose;
- design deterministic metrics for every capability and feature;
- fold the new metrics dashboard into ping-mem's main UI if the UI exists;
- keep proving whether ping-mem is better than `rg` by returning relationships,
  edges, provenance, freshness, source anchors, and denominator evidence.

## Current Evidence

The generated live scorecard is at:

- `docs/evidence/ground-up-local-trust/capability-scorecard-live/scorecard.html`
- `docs/evidence/ground-up-local-trust/capability-scorecard-live/scorecard.json`
- `docs/evidence/ground-up-local-trust/capability-scorecard-live/scorecard.md`

The generator is:

- `scripts/capability-scorecard.mjs`

Current scorecard result:

- Overall: `94% yellow`, `49/52` metrics passing.
- `ping-mem relationship lift over rg`: green.
- `rg baseline remains available`: green.
- `Live ping-mem discovery path`: red.
- `CAP-2 Agent Reachability`: yellow.

Direct live runtime proof from the previous session:

- `/health` returned top-level `status: "ok"`.
- `/health.components.neo4j` returned `not_configured`.
- `/health.components.qdrant` returned `healthy`.
- `/api/v1/codebase/projects?scope=registered` returned
  `HTTP 503 Service Unavailable` with `Ingestion service not configured`.
- `/Users/umasankr/.codex/bin/ping-mem-codex codebase projects --scope registered --json`
  failed with `HTTP 503: Ingestion service not configured`.
- Docker containers were up and healthy for `ping-mem`, `ping-mem-neo4j`, and
  `ping-mem-qdrant`.
- `docker inspect ping-mem` showed Neo4j and Qdrant environment variables
  present.
- `docker logs ping-mem` showed the current process started after five failed
  Neo4j connection attempts and then disabled ingestion/graph features.

Interpretation:

ping-mem currently has a healthy shell with a missing Neo4j-backed ingestion
brain. The graph-answer design is not the next blocker; live runtime readiness
and health honesty are.

## GitHub Tracking

Meta issue:

- `#135` Meta: complete capability metrics dashboard and live discovery readiness
  - `https://github.com/usorama/ping-mem/issues/135`

Child issues:

- `#136` Restore live ingestion service readiness and honest health semantics
- `#137` Inventory original ping-mem capabilities and map outcomes to metrics
- `#138` Expand capability scorecard metrics to cover every capability and feature
- `#139` Fold metrics dashboard into ping-mem main UI
- `#140` Benchmark ping-mem vs rg for relationship lift across approved roots

## Repo State To Preserve

The dirty worktree includes prior trust-rebuild work plus S017 and scorecard
work. Do not reset or revert it.

Important files added or changed in the recent work:

- `src/graph/StructuredKnowledgeGraph.ts`
- `src/graph/index.ts`
- `src/http/rest-server.ts`
- `src/validation/api-schemas.ts`
- `src/cli/agent-trust.ts`
- `src/cli/commands/agent.ts`
- `src/graph/__tests__/StructuredKnowledgeGraph.test.ts`
- `src/http/__tests__/agent-rest.test.ts`
- `src/cli/__tests__/agent-trust.test.ts`
- `scripts/capability-scorecard.mjs`
- `docs/evidence/ground-up-local-trust/S017-structured-knowledge-graph.md`
- `docs/evidence/ground-up-local-trust/S017-structured-knowledge-graph/`
- `docs/evidence/ground-up-local-trust/capability-scorecard/`
- `docs/evidence/ground-up-local-trust/capability-scorecard-live/`

## Verification Already Run

For S017:

- `bun test src/graph/__tests__/StructuredKnowledgeGraph.test.ts`
- `bun test src/http/__tests__/agent-rest.test.ts`
- `bun test src/cli/__tests__/agent-trust.test.ts`
- `bun run typecheck`
- `bun test src/graph src/http src/cli`
- `git diff --check`

For scorecard:

- `node --check scripts/capability-scorecard.mjs`
- `node scripts/capability-scorecard.mjs`
- `node scripts/capability-scorecard.mjs --live --out-dir=docs/evidence/ground-up-local-trust/capability-scorecard-live`
- `git diff --check`

## Recommended Next Steps

1. Start with `#136`: restore live ingestion service readiness and health honesty.
   The likely first recovery is restarting/recovering the `ping-mem` service after
   Neo4j is reachable, then rerunning the live scorecard. Do not claim success
   until the approved wrapper and `/api/v1/codebase/projects?scope=registered`
   pass.

2. Tighten health semantics. A top-level `ok` while Neo4j is `not_configured`
   should not be allowed for capability claims that depend on codebase/graph
   ingestion. Health, doctor/status, and scorecard need to agree.

3. Execute `#137` before expanding code further: inventory original ping-mem
   capabilities from docs/code. Include memory, codebase ingestion/search,
   graph/relationships, sessions, events, mining/dreaming, diagnostics, health,
   UI, CLI, MCP/proxy boundaries, recovery, and docs/operator truth.

4. Execute `#138`: extend the scorecard model so every capability and feature has
   a deterministic metric, evidence path, live/evidence-only status, and claim
   boundary.

5. Execute `#139`: fold the dashboard into the main ping-mem UI if the UI exists.
   Current UI paths include `src/http/ui/*`; inspect existing navigation before
   choosing the route.

6. Execute `#140`: only after live ingestion is green, run side-by-side `rg`
   versus ping-mem benchmarks across approved roots such as `/projects/vunderstory`
   and `/projects/codex-corpus`.

## Memory Note

An official Codex native memory-write tool was searched for with
`tool_search` using `Codex native memory update save remember memories`, but no
memory-write tool was exposed. This repo-local continuation package and GitHub
tracker are the durable handoff artifacts for reboot.

## Startup Message For Next Session

Use this when resuming:

```text
I am continuing the capability metrics and live discovery closure work from
continuation_20260503_111217.md. I will start by restoring live ingestion
service readiness and health honesty, then inventory all original ping-mem
capabilities, expand the scorecard metrics, integrate the dashboard into the UI,
and rerun rg-vs-ping-mem relationship-lift benchmarks.
```

<!-- CONTINUATION PROMPT END -->
